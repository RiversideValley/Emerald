<Page
    x:Class="Emerald.WinUI.Views.Home.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animations="using:CommunityToolkit.WinUI.UI.Animations"
    xmlns:core="using:Emerald.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Emerald.WinUI.Helpers"
    xmlns:settings="using:Emerald.WinUI.Helpers.Settings"
    xmlns:local="using:Emerald.WinUI.Views.Home"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Emerald.WinUI.Models"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="MCVersionTemplate" x:DataType="models:MinecraftVersion">
            <Grid>
                <Grid.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition/>
                    </TransitionCollection>
                </Grid.ChildrenTransitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image
                    Height="40"
                    VerticalAlignment="Top"
                    Source="{x:Bind BlockImageLocation}"
                    Stretch="Fill" />
                <StackPanel
                    Grid.Column="1"
                    Margin="5,0,0,0"
                    VerticalAlignment="Center">
                    <TextBlock
                        FontWeight="SemiBold"
                        Text="{x:Bind DisplayVersion}"
                        TextTrimming="CharacterEllipsis" />
                    <TextBlock
                        Margin="0,0,0,8"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        TextTrimming="CharacterEllipsis"
                        Visibility="{x:Bind DescriptionVisibility}">
                        <Run Text="{x:Bind Type.Value}" />
                        |<Run Text="{x:Bind Version}" />
                    </TextBlock>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <SplitView
        x:Name="paneVersions"
        DisplayMode="Inline"
        OpenPaneLength="300"
        CornerRadius="7"
        PaneBackground="{ThemeResource LayerFillColorDefaultBrush}"
        PanePlacement="Left">
        <SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel
                        Grid.Column="0"
                        Margin="10"
                        Orientation="Vertical">
                        <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="{helpers:Localize Name=PickVer}" />
                        <TextBlock
                            x:Name="txtVerOfflineMode"
                            VerticalAlignment="Bottom"
                            FontSize="13"
                            FontWeight="SemiBold"
                            Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}"
                            Text="{helpers:Localize Name=OfflineMode}" />
                    </StackPanel>
                    <Button
                        x:Name="btnCloseVerPane"
                        Grid.Column="1"
                        Margin="0,15,15,0"
                        Padding="0"
                        VerticalAlignment="Top">
                        <Button.Resources>
                            <SolidColorBrush x:Key="ButtonBackground" Color="Transparent" />
                            <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="Transparent" />
                            <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="{ThemeResource TextFillColorSecondary}" />
                            <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="Transparent" />

                            <SolidColorBrush x:Key="ButtonBorderBrush" Color="Transparent" />
                            <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="Transparent" />
                            <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="Transparent" />
                        </Button.Resources>
                        <FontIcon Glyph="&#xEDAE;" />
                    </Button>
                </Grid>
                <Grid Grid.Row="1" Margin="5,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <AutoSuggestBox
                        x:Name="txtbxFindVer"
                        Grid.Column="0"
                        Margin="0,0,0,5"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        PlaceholderText="Search for a version"
                        QueryIcon="Find"
                        TextChanged="txtbxFindVer_TextChanged" />
                    <DropDownButton
                        x:Name="btnVerSort"
                        Grid.Column="1"
                        Margin="5,0,0,0">
                        <FontIcon Glyph="&#xE16E;" />
                        <DropDownButton.Flyout>
                            <MenuFlyout>
                                <ToggleMenuFlyoutItem Click="tglMitVerSort_Click" IsChecked="{x:Bind settings:SettingsSystem.Settings.Minecraft.MCVerionsConfiguration.Release, Mode=TwoWay}" Text="{helpers:Localize CustomName=Release}"/>
                                <ToggleMenuFlyoutItem Click="tglMitVerSort_Click" Text="{helpers:Localize CustomName=Snapshot}" IsChecked="{x:Bind settings:SettingsSystem.Settings.Minecraft.MCVerionsConfiguration.Snapshot, Mode=TwoWay}"/>
                                <ToggleMenuFlyoutItem Click="tglMitVerSort_Click" Text="{helpers:Localize CustomName=OldBeta}" IsChecked="{x:Bind settings:SettingsSystem.Settings.Minecraft.MCVerionsConfiguration.OldBeta, Mode=TwoWay}"/>
                                <ToggleMenuFlyoutItem Click="tglMitVerSort_Click" Text="{helpers:Localize CustomName=OldAlpha}" IsChecked="{x:Bind settings:SettingsSystem.Settings.Minecraft.MCVerionsConfiguration.OldAlpha, Mode=TwoWay}"/>
                                <ToggleMenuFlyoutItem Click="tglMitVerSort_Click" Text="{helpers:Localize CustomName=Custom}" IsChecked="{x:Bind settings:SettingsSystem.Settings.Minecraft.MCVerionsConfiguration.Custom, Mode=TwoWay}"/>
                            </MenuFlyout>
                        </DropDownButton.Flyout>
                    </DropDownButton>
                </Grid>
                <TreeView
                    x:Name="treeVer"
                    Grid.Row="2"
                    CanDragItems="False"
                    CanReorderItems="False"
                    ItemInvoked="treeVer_ItemInvoked"
                    SelectionMode="None">
                    <TreeView.ItemTemplate>
                        <DataTemplate x:DataType="models:MinecraftVersion">
                            <TreeViewItem
                                MinHeight="0"
                                Margin="0,0,5,0"
                                Padding="0"
                                x:DefaultBindMode="OneWay"
                                Content="{x:Bind}"
                                ContentTemplate="{StaticResource MCVersionTemplate}"
                                ItemsSource="{x:Bind SubVersions}" />
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
                <TextBlock
                    x:Name="txtEmptyVers"
                    Grid.Row="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Text="{helpers:Localize Name=Empty}"
                    Visibility="Collapsed" />
            </Grid>
        </SplitView.Pane>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="1*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  TODO: Add click event (to account settings page) and data template for username  -->
            <Button
                x:Name="AccountButton"
                Grid.Row="0"
                Margin="30,20,30,30"
                Padding="10,2"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="{ThemeResource ButtonBackground}"
                Click="AccountButton_Click"
                Style="{ThemeResource ButtonRevealStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontSize="16"
                        FontWeight="Bold"
                        Text="Account name (e.g. Noob)" />
                    <!--  TODO: Add offline visibilty binding for offline accounts  -->
                    <TextBlock
                        x:Name="IsOffline"
                        Margin="10,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="12"
                        Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}"
                        Text="Offline"
                        Visibility="Visible" />
                </StackPanel>
            </Button>

            <Button
                Grid.Row="0"
                Height="27.2"
                Margin="30,20,30,30"
                Padding="10,3"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="{ThemeResource ButtonBackground}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon
                        VerticalAlignment="Center"
                        FontSize="18"
                        Glyph="&#xF57F;" />
                    <TextBlock
                        Margin="10,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="14"
                        Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}"
                        Text="News" />
                </StackPanel>
            </Button>

            <Grid
                Grid.Row="2"
                Height="60"
                Margin="30,40"
                VerticalAlignment="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" MaxWidth="230" />
                    <ColumnDefinition Width="0.10*" />
                    <ColumnDefinition Width="1*" MaxWidth="250" />
                </Grid.ColumnDefinitions>
                <DropDownButton
                    x:Name="VersionButton"
                    Grid.Column="0"
                    Padding="5,0,10,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Click="VersionButton_Click"
                    ContentTemplate="{StaticResource MCVersionTemplate}" />
                <Button
                    x:Name="LaunchButton"
                    Grid.Column="2"
                    Click="LaunchButton_Click"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    FontSize="16"
                    Style="{ThemeResource AccentButtonStyle}">
                    <TextBlock
                        Text="{helpers:Localize Name=LaunchMC}"
                        HorizontalAlignment="Center"
                        FontSize="16"
                        FontWeight="Bold" />
                </Button>
            </Grid>
            <Grid
                Grid.Row="1"
                Margin="30,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Visibility="Visible">
                <FlipView
                    x:Name="FlipView"
                    Background="{ThemeResource ButtonBackground}"
                    BorderBrush="{ThemeResource ButtonBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    ItemsSource="{Binding NewsViews}" />

                <!--<muxc:PipsPager
                Margin="0,70,0,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                NumberOfPages="{Binding NewsViews.Count}"
                SelectedPageIndex="{x:Bind Path=FlipView.SelectedIndex, Mode=TwoWay}" />-->
            </Grid>
        </Grid>
    </SplitView>
</Page>
