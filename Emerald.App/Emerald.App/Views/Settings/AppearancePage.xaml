<Page
    x:Class="Emerald.WinUI.Views.Settings.AppearancePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Emerald.WinUI.Views.Settings"
    xmlns:uc="using:Emerald.WinUI.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:Emerald.WinUI.Helpers"
    xmlns:SS="using:Emerald.WinUI.Helpers.Settings" 
    xmlns:windowsui="using:Windows.UI"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Spacing="3" ChildrenTransitions="{StaticResource EntranceThemeTransitionStaggeringEnabled}">
            <uc:Expander 
            Icon="&#xE790;"
            ExpanderStyle="Static" 
            Title="{helpers:LocalizeString CustomName=ColorMode,RESW=Settings}" 
            Description="{helpers:LocalizeString CustomName=ColorModeDescription,RESW=Settings}">
                <uc:Expander.HeaderControls>
                    <ComboBox SelectedIndex="{x:Bind SS:SettingsSystem.Settings.App.Appearance.Theme,Mode=TwoWay}">
                        <ComboBoxItem Content="{helpers:LocalizeString CustomName=SystemDefault,RESW=Settings}"/>
                        <ComboBoxItem Content="{helpers:LocalizeString CustomName=Light,RESW=Settings}"/>
                        <ComboBoxItem Content="{helpers:LocalizeString CustomName=Dark,RESW=Settings}"/>
                    </ComboBox>
                </uc:Expander.HeaderControls>
            </uc:Expander>
            <uc:Expander
            Icon="&#xE790;"
            ExpanderStyle="Static"
            Title="{helpers:LocalizeString CustomName=MicaType,RESW=Settings}" 
            Description="{helpers:LocalizeString CustomName=MicaTypeDescription,RESW=Settings}">
                <uc:Expander.HeaderControls>
                    <ComboBox SelectedIndex="{x:Bind SS:SettingsSystem.Settings.App.Appearance.MicaType,Mode=TwoWay}">
                        <ComboBoxItem Content="Mica"/>
                        <ComboBoxItem Content="Mica Alt"/>
                    </ComboBox>
                </uc:Expander.HeaderControls>
            </uc:Expander>
            <uc:Expander
                Icon="&#xE771;"
                ExpanderStyle="Default" 
                Title="{helpers:LocalizeString CustomName=WindowTintColor,RESW=Settings}"
                Description="{helpers:LocalizeString CustomName=WindowTintColorDescription,RESW=Settings}">
                <uc:Expander.HeaderControls>
                    <ComboBox SelectedIndex="{x:Bind SS:SettingsSystem.Settings.App.Appearance.MicaTintColor,Mode=TwoWay}">
                        <ComboBoxItem Content="{helpers:LocalizeString CustomName=NoColor,RESW=Settings}"/>
                        <ComboBoxItem Content="{helpers:LocalizeString CustomName=AccentColor,RESW=Settings}"/>
                        <ComboBoxItem Content="{helpers:LocalizeString CustomName=CustomColor,RESW=Settings}"/>
                    </ComboBox>
                </uc:Expander.HeaderControls>
                <uc:Expander.Controls>
                    <GridView
                        x:Name="GVColorList"
                        ItemsSource="{x:Bind TintColorsList}"
                        SelectionChanged="GVColorList_SelectionChanged">
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="Margin" Value="1"/>
                            </Style>
                        </GridView.ItemContainerStyle>

                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid
                                    MaximumRowsOrColumns="12"
                                    Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="windowsui:Color">
                                <Border
                                    Width="45"
                                    Height="45"
                                    CornerRadius="5">
                                    <Border.Background>
                                        <SolidColorBrush Color="{x:Bind}" />
                                    </Border.Background>
                                </Border>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                </uc:Expander.Controls>
            </uc:Expander>
        </StackPanel>

    </Grid>
</Page>