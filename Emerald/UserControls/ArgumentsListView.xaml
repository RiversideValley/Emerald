<UserControl
    x:Class="Emerald.UserControls.ArgumentsListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

  <Grid>
    <StackPanel>
      <StackPanel Margin="0,5" Orientation="Horizontal">
        <Button x:Name="btnAdd" Padding="7" Click="btnAdd_Click">
          <FontIcon Glyph="&#xe710;" />
        </Button>
        <Button x:Name="btnRemove" Margin="5,0,0,0" Padding="7" Click="btnRemove_Click">
          <FontIcon Glyph="&#xe738;" />
        </Button>
      </StackPanel>

      <ListView x:Name="view" SelectionChanged="view_SelectionChanged">
        <ListView.ItemContainerTransitions>
          <TransitionCollection>
            <AddDeleteThemeTransition />
          </TransitionCollection>
        </ListView.ItemContainerTransitions>

        <ListView.ItemTemplate>
          <DataTemplate x:DataType="x:String">
            <TextBox
                            Margin="-9,-4"
                            VerticalAlignment="Stretch"
                            Background="Transparent"
                            BorderThickness="0"
                            PlaceholderText="Empty"
                            Text="{x:Bind Mode=TwoWay}" />
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackPanel>
  </Grid>
</UserControl>
