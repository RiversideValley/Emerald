<Page
    x:Class="Emerald.Views.AccountsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cn="using:CommunityToolkit.WinUI.Controls"
    xmlns:conv="using:Emerald.Helpers.Converters"
    xmlns:auth="using:Emerald.CoreX.Services.Auth"
    xmlns:models="using:Emerald.CoreX.Models"
    xmlns:helpers="using:Emerald.Helpers"
    mc:Ignorable="d">

  <Page.Resources>
    <conv:BoolToVisibility x:Key="BoolToVis"/>
    <conv:BoolToVisibility x:Key="InverseBoolToVis" Reversed="True"/>
    <conv:CountToVisibilityConverter x:Key="CountToVis"/>
    <conv:InverseBoolConverter x:Key="InverseBoolConverter"/>
    <conv:AccountAvailabilityConverter x:Key="AccountAvailabilityConverter"/>
    <conv:AccountLastUsedConverter x:Key="AccountLastUsedConverter"/>

    <DataTemplate x:Key="AccountItemTemplate" x:DataType="models:EAccount" x:DefaultBindMode="OneWay">
      <cn:SettingsCard
        Margin="0,4"
        ActionIconToolTip="{helpers:Localize KeyName=Select}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        Click="AccountCard_Click"
        IsClickEnabled="{x:Bind IsSelected, Converter={StaticResource InverseBoolConverter}, Mode=OneWay}"
        Tag="{x:Bind}">
        <cn:SettingsCard.Header>
          <StackPanel Spacing="4">
            <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind ProviderDisplayName}"/>
            </StackPanel>
            <TextBlock Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}">
              <Run Text="{helpers:Localize KeyName=LastUsed}"/>
              <Run Text=" "/>
              <Run Text="{x:Bind LastUsed, Converter={StaticResource AccountLastUsedConverter}}"/>
            </TextBlock>
            <TextBlock Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind Availability, Converter={StaticResource AccountAvailabilityConverter}}"/>
            <TextBlock Opacity="0.7" Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind AvailabilityMessage}" Visibility="{x:Bind HasAvailabilityMessage, Converter={StaticResource BoolToVis}, Mode=OneWay}" TextWrapping="WrapWholeWords"/>
          </StackPanel>
        </cn:SettingsCard.Header>
        <StackPanel Orientation="Horizontal" Spacing="8">
          <TextBlock
            VerticalAlignment="Center"
            FontSize="14"
            FontWeight="SemiBold"
            Foreground="{ThemeResource SystemAccentColor}"
            Text="{helpers:Localize KeyName=Selected}"
            Visibility="{x:Bind IsSelected, Converter={StaticResource BoolToVis}, Mode=OneWay}" />
          <Button
            ToolTipService.ToolTip="{helpers:Localize KeyName=RefreshAccount}"
            Click="RefreshAccount_Click"
            Tag="{x:Bind}">
            <FontIcon Glyph="&#xE72C;"/>
          </Button>
          <ItemsControl ItemsSource="{x:Bind ProviderActions}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" Spacing="8"/>
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="auth:AccountProviderActionDescriptor">
                <Button ToolTipService.ToolTip="{x:Bind DisplayName}" Click="ProviderAction_Click" Tag="{x:Bind}">
                  <FontIcon Glyph="&#xE8A7;"/>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          <Button
            ToolTipService.ToolTip="{helpers:Localize KeyName=RemoveAccount}"
            Click="RemoveAccount_Click"
            Tag="{x:Bind}">
            <FontIcon Glyph="&#xE74D;" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
          </Button>
        </StackPanel>
      </cn:SettingsCard>
    </DataTemplate>

  </Page.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Padding="24,24,24,12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <StackPanel Grid.Column="0">
        <TextBlock Text="{helpers:Localize KeyName=Accounts}" Style="{StaticResource TitleTextBlockStyle}"/>
        <TextBlock Text="{helpers:Localize KeyName=ManageSignedInAccounts}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
      </StackPanel>

      <StackPanel Grid.Column="1" Spacing="8" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
          <CommandBar Background="Transparent" DefaultLabelPosition="Right">
            <AppBarElementContainer>
              <Button x:Name="AddAccountButton"
                      Click="AddAccountButton_OnClick"
                      Content="{helpers:Localize KeyName=AddAccount}"
                      Visibility="{x:Bind ViewModel.IsLoginInProgress, Mode=OneWay, Converter={StaticResource InverseBoolToVis}}"/>
            </AppBarElementContainer>
          </CommandBar>
        </StackPanel>
      </StackPanel>
    </Grid>

    <ScrollViewer Grid.Row="1" Padding="20,0,20,24" VerticalScrollBarVisibility="Auto">
      <StackPanel Spacing="16">

        <Border
          Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
          BorderThickness="1"
          CornerRadius="8"
          Padding="12"
          Visibility="{x:Bind ViewModel.HasLoadError, Mode=OneWay, Converter={StaticResource BoolToVis}}">
          <TextBlock Text="{x:Bind ViewModel.LoadErrorMessage, Mode=OneWay}" TextWrapping="WrapWholeWords"/>
        </Border>

        <StackPanel Spacing="12" HorizontalAlignment="Center" Margin="0,48,0,0" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVis}}">
          <ProgressRing IsActive="True"/>
          <TextBlock Text="{x:Bind ViewModel.LoadingMessage, Mode=OneWay}" TextAlignment="Center"/>
          <Button
            HorizontalAlignment="Center"
            Command="{x:Bind ViewModel.CancelLoginCommand}"
            Content="{helpers:Localize KeyName=CancelLogin}"
            Visibility="{x:Bind ViewModel.IsLoginInProgress, Mode=OneWay, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>

        <ItemsRepeater
                    ItemsSource="{x:Bind ViewModel.Accounts, Mode=OneWay}"
                    ItemTemplate="{StaticResource AccountItemTemplate}"
                    Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource InverseBoolToVis}}">
          <ItemsRepeater.Layout>
            <StackLayout Spacing="4"/>
          </ItemsRepeater.Layout>
        </ItemsRepeater>

        <StackPanel
                    HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,64,0,0" Spacing="12"
                    Visibility="{x:Bind ViewModel.Accounts.Count, Mode=OneWay, Converter={StaticResource CountToVis}, ConverterParameter=0}">
          <FontIcon Glyph="&#xE77B;" FontSize="48" Opacity="0.5"/>
          <TextBlock Text="{helpers:Localize KeyName=NoAccountsFound}" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center"/>
          <TextBlock Text="{helpers:Localize KeyName=AddAccountHint}" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" HorizontalAlignment="Center"/>
        </StackPanel>
      </StackPanel>
    </ScrollViewer>

  </Grid>
</Page>
